npm i motion --legacy-peer-deps

->motion package install karta hai,
👉 aur peer dependency conflicts ko ignore kar deta hai.
👉 React me smooth animations / scroll effects ke liye use hoti hai

--legacy-peer-deps
-> “Agar dependency versions me conflict ho,toh old npm style follow karo aur error mat do.”